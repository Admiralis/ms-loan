spring:
  data:
    mongodb:
      uri: mongodb://mongoadmin:secret@admiralis-db:27017/loans?authSource=admin
      database: loans
  devtools:
    restart:
      enabled: true
      poll-interval: 2s
      quiet-period: 1s
    livereload:
        port: 35731
springdoc:
  swagger-ui:
    path: /api-docs

server:
  port: 8082

ms:
  course:
    url: http://ms-course:8080/api/courses
  student:
    url: http://ms-course:8080/api/students
  computer:
    url: http://ms-computer:8081/api/computers
